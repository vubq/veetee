# VeeTee runtime dependencies - pinned from venv 2026-09-09 (Python 3.12).
# Install: ../../venv/bin/pip install -r requirements.txt
# CUDA: torch==2.6.0+cu124 can CUDA 12.4. Neu may CPU-only, thay bang torch CPU
# tu https://pytorch.org/get-started/locally/ truoc khi pip install -r file nay.
# nemo_toolkit[asr] keo them nhieu deps phu (xem pip freeze day du khi can lock tuyet doi).

vieneu==3.6.4
websockets==17.1
aiohttp==3.14.3
numpy==2.5.3
soundfile==0.14.0
scipy==1.18.1
opuslib-next==1.3.1
PyYAML==6.0.3
torch==2.6.0+cu124
transformers==5.16.1
accelerate==1.14.0
soxr==1.1.0
onnxruntime==1.29.0
nemo_toolkit[asr]==3.0.0
# HTTP dashboard / management API (da co trong venv, giu pin de tai hien).
fastapi==0.141.1
uvicorn==0.52.4
